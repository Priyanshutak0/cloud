services:
  ejschallenge:
    image: ejschallenge
    build:
      context: Task2/blogWebsite-main/blogWebsite-main
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
